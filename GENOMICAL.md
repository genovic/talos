# Genomical Changes

## Workflow changes
* Added a [buildspec.yml](buildspec.yml)
* Added a [config-template.toml](nextflow/inputs/config-template.toml) for use as a template when updating the config.toml file.

## Docker changes
* Install AWS CLI v2 to allow for AWS interactions.
